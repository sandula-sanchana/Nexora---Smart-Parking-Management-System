spring:
  application:
    name: parking-service

  datasource:
    url: jdbc:mysql://localhost:3306/parking_db
    username: root
    password: Sandula#2026$MySQL!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082
  servlet:
    context-path: /parking_service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/